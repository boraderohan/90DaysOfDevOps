# Express + MongoDB App

A simple Node.js Express app connected to MongoDB, containerized with Docker.

## íº€  How to Run

1. Clone this repo
2. Create `.env` file:
   ```env
   MONGODB_URI=mongodb://mongo:27017/myapp
   NODE_ENV=production
   PORT=3000
